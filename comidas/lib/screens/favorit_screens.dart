import 'package:flutter/material.dart';

class FavoritScreens extends StatelessWidget {
  const FavoritScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Minhas comidas favoritas'),
    );
  }
}